from kehuo/debian_nginx:conda_git_vim

WORKDIR /project

ADD ./ ./

# RUN source ~/.bashrc \
#    && conda activate myenv \
#    && pip install --upgrade pip && pip install Cython && pip install path.py \
# 	 && pip install -r ./requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

