; 由于前后端的分离, 不需要将后台部署到ssl端口, 只需要监听 80 端口即可. 所以将以下ssl的nginx配置注释掉了.
; server {
;     #listen 80;
;     listen       443 ssl;
;     server_name  localhost;
;
;     ssl_certificate /etc/nginx/cert/kevinhuo_cool.pem;
;     ssl_certificate_key /etc/nginx/cert/kevinhuo_cool.key;
;
;     ssl_session_timeout 10m;
;     ssl_prefer_server_ciphers on;
;
;     #charset koi8-r;
;     #access_log  /var/log/nginx/host.access.log  main;
;
;     location / {
;         #root   /usr/share/nginx/html;
;         #index  index.html index.htm;
;         proxy_pass http://127.0.0.1:5000;
;     }
;
;     #error_page  404              /usr/share/nginx/html;
;
;     # redirect server error pages to the static page /50x.html
;     #
;     error_page   500 502 503 504  /50x.html;
;     location = /50x.html {
;         root   /usr/share/nginx/html;
;     }
; }
;
;
; server {
;     listen 80;
;     server_name localhost;
;     #return 301 https://$server_name$request_uri;
;     #rewrite ^(.*) https://$host
;     rewrite ^(.*) https://$host$1 permanent;
; }

# 前后端分离的情况下, 使用以下 80 端口的配置即可.
server {
    listen 80;
    server_name  localhost;

    # ssl_certificate /etc/nginx/cert/kevinhuo_cool.pem;
    # ssl_certificate_key /etc/nginx/cert/kevinhuo_cool.key;

    # ssl_session_timeout 10m;
    # ssl_prefer_server_ciphers on;

    #charset koi8-r;
    #access_log  /var/log/nginx/host.access.log  main;

    location / {
        #root   /usr/share/nginx/html;
        #index  index.html index.htm;
        proxy_pass http://127.0.0.1:5000;
    }

    #error_page  404              /usr/share/nginx/html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}